# src/api — stub da saída de mídia

Desligado por padrão. Ver ../../docs/API.md para habilitar (REST local ou webhook).
`output.js` só emite o evento interno `captura` enquanto `outputApi.enabled` for false.
