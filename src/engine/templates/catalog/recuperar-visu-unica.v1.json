{
  "templateId": "recuperar-visu-unica",
  "templateVersion": 1,
  "name": "Recuperar visualização única",
  "description": "O comando que deu origem a este projeto: responda uma mídia de visualização única com o comando e ela é reenviada como mídia normal, para onde você escolher. Funciona porque o WhatsApp deixa uma cópia recuperável da mídia dentro da própria citação.",
  "category": "utilitarios",
  "dependsOn": [],
  "requiredCapabilities": [],
  "warnings": [
    {
      "level": "info",
      "text": "Só funciona respondendo a mídia de visualização única. O WhatsApp não entrega o conteúdo direto ao bot — a cópia recuperável só existe dentro da mensagem respondida."
    }
  ],
  "parameters": [
    {
      "key": "command",
      "label": "Comando",
      "type": "string",
      "default": "/recover",
      "help": "A palavra que você digita respondendo a mídia de visualização única."
    },
    {
      "key": "destination",
      "label": "Para onde reenviar",
      "type": "string",
      "default": "saved_messages",
      "help": "Use \"saved_messages\" para a sua própria conversa (Mensagens salvas) ou \"same_chat\" para reenviar na mesma conversa onde o comando foi usado."
    },
    {
      "key": "caption",
      "label": "Legenda da mídia recuperada",
      "type": "string",
      "default": "Recuperado de {{sender.id}}",
      "help": "Texto que acompanha a mídia reenviada. Aceita variáveis do sistema."
    },
    {
      "key": "notFoundText",
      "label": "Aviso quando não achar",
      "type": "string",
      "default": "Responda uma mídia de visualização única com este comando pra eu recuperar.",
      "help": "Texto enviado quando o comando é usado sem responder uma visualização única."
    },
    {
      "key": "poolId",
      "label": "Pool que responde",
      "type": "string",
      "help": "Identificador do pool de números que vai executar este comando."
    }
  ],
  "documentTemplate": {
    "schemaVersion": 1,
    "enabled": true,
    "name": "Recuperar visualização única",
    "display": {
      "menuLabel": "{{params.command}}",
      "menuDescription": "Recupera mídia de visualização única"
    },
    "scope": {
      "include": [],
      "exclude": []
    },
    "inputPolicy": {
      "acceptedMessageKinds": [
        "text"
      ],
      "historyPolicy": "live_only"
    },
    "responder": {
      "strategy": "weighted_rendezvous",
      "poolId": "{{params.poolId}}"
    },
    "flow": {
      "nodes": [
        {
          "id": "gatilho-comando",
          "type": "trigger.command",
          "config": {
            "command": "{{params.command}}",
            "match": "exact_or_args",
            "allowFrom": "external"
          }
        },
        {
          "id": "recuperar",
          "type": "action.whatsapp.recover",
          "config": {
            "destination": "{{params.destination}}",
            "caption": "{{params.caption}}",
            "notFoundText": "{{params.notFoundText}}"
          }
        }
      ],
      "edges": [
        {
          "from": "gatilho-comando",
          "to": "recuperar",
          "on": "matched"
        }
      ]
    }
  }
}
