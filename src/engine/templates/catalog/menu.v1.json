{
  "templateId": "menu",
  "templateVersion": 1,
  "name": "Menu de comandos",
  "description": "Lista os comandos disponíveis naquela conversa. Cada grupo vê só o que vale nele, e o texto pode ser diferente em cada grupo. Vem junto com o comando de configuração, que permite mudar esse texto pelo próprio WhatsApp.",
  "category": "utilitarios",
  "dependsOn": [
    "config-menu"
  ],
  "requiredCapabilities": [],
  "warnings": [
    {
      "level": "compatibility",
      "text": "No formato com botões, o menu NÃO aparece no WhatsApp Business e pode não aparecer em versões antigas do aplicativo. Nesses casos o sistema envia o menu como texto automaticamente, então ninguém fica sem resposta."
    }
  ],
  "parameters": [
    {
      "key": "command",
      "label": "Comando",
      "type": "string",
      "default": "/menu",
      "help": "A palavra que a pessoa digita para ver a lista de comandos."
    },
    {
      "key": "allowFrom",
      "label": "Quem pode acionar",
      "type": "string",
      "default": "external",
      "help": "\"any\" responde também às SUAS próprias mensagens, do número onde o bot roda — é o normal para comando de uso pessoal. \"external\" faz o bot ignorar o próprio número, que é o padrão seguro quando outras pessoas usam o comando."
    },
    {
      "key": "header",
      "label": "Primeira linha do menu",
      "type": "string",
      "default": "Comandos disponíveis:",
      "help": "Pode ser trocada depois, por conversa, usando o comando de configuração."
    },
    {
      "key": "emptyText",
      "label": "Quando não houver comando",
      "type": "string",
      "default": "Nenhum comando disponível por aqui ainda.",
      "help": "Resposta quando nenhuma automação está publicada para aquela conversa."
    },
    {
      "key": "poolId",
      "label": "Pool que responde",
      "type": "string",
      "help": "Identificador do pool de números que responde este comando."
    }
  ],
  "documentTemplate": {
    "schemaVersion": 1,
    "enabled": true,
    "name": "Menu de comandos",
    "display": {
      "menuLabel": "{{params.command}}",
      "menuDescription": "Mostra esta lista de comandos"
    },
    "scope": {
      "include": [],
      "exclude": []
    },
    "inputPolicy": {
      "acceptedMessageKinds": [
        "text"
      ],
      "historyPolicy": "live_only"
    },
    "responder": {
      "strategy": "weighted_rendezvous",
      "poolId": "{{params.poolId}}"
    },
    "flow": {
      "nodes": [
        {
          "id": "gatilho-comando",
          "type": "trigger.command",
          "config": {
            "command": "{{params.command}}",
            "match": "exact",
            "allowFrom": "{{params.allowFrom}}"
          }
        },
        {
          "id": "mostrar-menu",
          "type": "action.menu.render",
          "config": {
            "header": "{{params.header}}",
            "emptyText": "{{params.emptyText}}"
          }
        }
      ],
      "edges": [
        {
          "from": "gatilho-comando",
          "to": "mostrar-menu",
          "on": "matched"
        }
      ]
    }
  }
}
