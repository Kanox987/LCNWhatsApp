{
  "templateId": "ping",
  "templateVersion": 1,
  "name": "Ping (teste de latência)",
  "description": "Responde a um comando com \"pong\" e o tempo real de processamento em milissegundos. Bom primeiro teste pra confirmar que uma automação nova está publicada e respondendo de verdade.",
  "category": "utilitarios",
  "dependsOn": [],
  "requiredCapabilities": [],
  "parameters": [
    {
      "key": "command",
      "label": "Comando",
      "type": "string",
      "default": "/ping",
      "help": "A palavra que a pessoa digita pra acionar esta automação."
    },
    {
      "key": "allowFrom",
      "label": "Quem pode acionar",
      "type": "string",
      "default": "external",
      "help": "\"any\" responde também às SUAS próprias mensagens, do número onde o bot roda — é o normal para comando de uso pessoal. \"external\" faz o bot ignorar o próprio número, que é o padrão seguro quando outras pessoas usam o comando."
    },
    {
      "key": "poolId",
      "label": "Pool que responde",
      "type": "string",
      "help": "Identificador do pool de números que vai responder este comando. Crie um pool na aba Automações antes de instalar, se ainda não tiver um."
    }
  ],
  "documentTemplate": {
    "schemaVersion": 1,
    "enabled": true,
    "name": "Ping (teste de latência)",
    "scope": {
      "include": [],
      "exclude": []
    },
    "inputPolicy": {
      "acceptedMessageKinds": [
        "text"
      ],
      "historyPolicy": "live_only"
    },
    "responder": {
      "strategy": "weighted_rendezvous",
      "poolId": "{{params.poolId}}"
    },
    "flow": {
      "nodes": [
        {
          "id": "gatilho-comando",
          "type": "trigger.command",
          "config": {
            "command": "{{params.command}}",
            "match": "exact",
            "allowFrom": "{{params.allowFrom}}"
          }
        },
        {
          "id": "resposta-whatsapp",
          "type": "action.whatsapp.reply",
          "config": {
            "text": "pong ({{latencyMs}}ms)"
          }
        }
      ],
      "edges": [
        {
          "from": "gatilho-comando",
          "to": "resposta-whatsapp",
          "on": "matched"
        }
      ]
    }
  }
}
